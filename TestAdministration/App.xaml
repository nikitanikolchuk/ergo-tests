<Application x:Class="TestAdministration.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewModels="clr-namespace:TestAdministration.ViewModels"
             xmlns:views="clr-namespace:TestAdministration.Views"
             StartupUri="Views/MainWindow.xaml">
    <Application.Resources>
        <viewModels:ViewModelLocator x:Key="ViewModelLocator" />

        <DataTemplate DataType="{x:Type viewModels:MainViewModel}">
            <views:MainWindow />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:LoginScreenViewModel}">
            <views:LoginScreen />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:MainScreenViewModel}">
            <views:MainScreen />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:InitContentViewModel}">
            <views:InitContent />
        </DataTemplate>

        <LinearGradientBrush x:Key="LightBorderBrush" StartPoint="0.5,0" EndPoint="0.5,1">
            <GradientStop Offset="0.9" Color="#0F000000" />
            <GradientStop Offset="1" Color="#29000000" />
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="DarkBorderBrush" StartPoint="0.5,0" EndPoint="0.5,1">
            <GradientStop Offset="0.9" Color="#0F000000" />
            <GradientStop Offset="1" Color="#72000000" />
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="BlueBorderBrush" StartPoint="0.5,0" EndPoint="0.5,1">
            <GradientStop Offset="0.9" Color="#0F000000" />
            <GradientStop Offset="1" Color="#005FB8" />
        </LinearGradientBrush>
        
        <DropShadowEffect x:Key="WindowShadow"
                          BlurRadius="64"
                          Color="#30000000"
                          Direction="-90"
                          ShadowDepth="2"
                          Opacity="0.5"
                          RenderingBias="Quality"/>

        <Style x:Key="BaseTextStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="Foreground" Value="#E5000000" />
        </Style>

        <Style x:Key="TitleLarge"
               TargetType="TextBlock"
               BasedOn="{StaticResource BaseTextStyle}">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="40" />
        </Style>

        <Style x:Key="Subtitle"
               TargetType="TextBlock"
               BasedOn="{StaticResource BaseTextStyle}">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="20" />
        </Style>

        <Style x:Key="BodyLarge"
               TargetType="TextBlock"
               BasedOn="{StaticResource BaseTextStyle}">
            <Setter Property="FontSize" Value="18" />
        </Style>

        <Style x:Key="BodyStrong"
               TargetType="TextBlock"
               BasedOn="{StaticResource BaseTextStyle}">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="14" />
        </Style>

        <Style x:Key="Body"
               TargetType="TextBlock"
               BasedOn="{StaticResource BaseTextStyle}">
            <Setter Property="FontSize" Value="14" />
        </Style>

        <Style x:Key="FontIcon" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontFamily" Value="Segoe Fluent Icons" />
            <Setter Property="FontSize" Value="64" />
        </Style>

        <Style x:Key="InnerStandardTextBox" TargetType="TextBox">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="#E5000000" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>

        <Style x:Key="StandardTextBox" TargetType="TextBox">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Background" Value="#B3FFFFFF" />
            <Setter Property="BorderBrush" Value="{StaticResource DarkBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="4"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <Border x:Name="BlueBorder"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="Stretch"
                                        CornerRadius="4"
                                        BorderBrush="#005FB8"
                                        BorderThickness="0,0,0,2"
                                        Visibility="Hidden" />

                                <TextBox Margin="12,0"
                                         x:Name="InnerTextBox"
                                         Style="{StaticResource InnerStandardTextBox}"
                                         Text="{TemplateBinding Text}" />
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#7FF9F9F9" />
                            </Trigger>
                            <Trigger SourceName="InnerTextBox" Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#0F000000" />
                                <Setter Property="BorderThickness" Value="1,1,1,0" />
                                <Setter TargetName="BlueBorder" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StandardButton" TargetType="Button">
            <Setter Property="Width" Value="200" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Background" Value="#B3FFFFFF" />
            <Setter Property="Foreground" Value="#E5000000" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="4"
                                BorderBrush="{StaticResource LightBorderBrush}"
                                BorderThickness="1"
                                Background="{TemplateBinding Background}">
                            <TextBlock Style="{StaticResource Body}"
                                       Foreground="{TemplateBinding Foreground}"
                                       Text="{TemplateBinding Content}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Opacity" Value="0.8" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="AccentButton"
               TargetType="Button"
               BasedOn="{StaticResource StandardButton}">
            <Setter Property="Background" Value="#005FB8" />
            <Setter Property="Foreground" Value="White" />
        </Style>
    </Application.Resources>
</Application>