<UserControl x:Class="TestAdministration.Views.NavBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:TestAdministration.Views"
             xmlns:viewModels="clr-namespace:TestAdministration.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:MainViewModel}"
             d:DesignWidth="300"
             d:DesignHeight="1080">
    <DockPanel LastChildFill="False">
        <views:NavSection DockPanel.Dock="Top" Text="Testování">
            <StackPanel>
                <views:NavButton Text="Devítikolíkový test" />
                <views:NavButton Text="Purdue Pegboard Test" />
                <views:NavButton Text="Box and Block Test" />
            </StackPanel>
        </views:NavSection>
        <views:NavButton DockPanel.Dock="Top"
                         Text="Výsledky"
                         Command="{Binding ResultsButtonCommand}" />
        <views:NavSection DockPanel.Dock="Top" Text="Manuály">
            <StackPanel>
                <views:NavButton Text="Textové manuály"
                                 Command="{Binding TextManualsButtonCommand}" />
                <views:NavButton Text="Videomanuály"
                                 Command="{Binding VideoManualsButtonCommand}" />
            </StackPanel>
        </views:NavSection>
        <views:NavButton DockPanel.Dock="Bottom" Text="Odhlášení" />
        <views:NavButton DockPanel.Dock="Bottom" Text="Nastavení" />
        <views:NavButton DockPanel.Dock="Bottom" Text="Návod k použití" />
    </DockPanel>
</UserControl>